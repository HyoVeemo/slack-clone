import React from 'react';
import {Form, Label, Input, LinkContainer, Button, Header} from './style'

const SignUp = () => {
    return (
       <div id="container">
            <Header>Sleact</Header>
            <Form onSubmit={undefined}></Form>
        </div>
    )
}

export default SignUp;